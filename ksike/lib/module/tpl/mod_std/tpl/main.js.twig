/*
 * @author		{{ author }}
 * @package    	{{ package }}
 * @created		{{ date }}
 * @updated		{{ date }}
 * @copyright  	{{ copyright }}
 * @license    	{{ license }}
 * @version    	{{ version }}
 * */
class Main
{
    index(req, assist){
        return req;
    }

    preAction(req, assist) {
        //... do it
    }

    posAction(req, assist) {
        //... do it
    }
}
exports.Main = Main;